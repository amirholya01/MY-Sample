<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">Courses project</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" asp-area="Admin" asp-controller="Home" asp-action="Index">Admin</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(ViewBag.ControllerName == "Home" && ViewBag.ActionName == "Index" ? "active" : "")" aria-current="page" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(ViewBag.ControllerName == "Course" && ViewBag.ActionName == "CoursesList" ? "active" : "")" asp-area="" asp-controller="Course" asp-action="CoursesList">Courses</a>
                </li>
            </ul>
            <form class="d-flex" asp-area="" asp-controller="Course" asp-action="CoursesList" method="get">
                <input name="Title" class="form-control me-2" type="search" value="@Context.Request.Query["Title"]" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>